import { createSlice } from "@reduxjs/toolkit";

const initialState={
    index:null
}
const indexSlice=createSlice({
    name:'index',
    initialState,
    reducers:{
        setIndex(state,action){
            
        }
    }
})